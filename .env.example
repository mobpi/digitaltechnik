# ============================================
# Digitaltechnik Lernprogramm - Umgebungsvariablen
# ============================================
# Kopieren Sie diese Datei nach .env und passen Sie die Werte an
# NIEMALS .env in Git committen!

# E-Mail Konfiguration (optional)
# Falls nicht gesetzt, funktioniert das Programm ohne E-Mail-Versand

# SMTP Server
SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_SECURE=false

# SMTP Zugangsdaten
SMTP_USER=ihre-email@domain.at
SMTP_PASS=ihr-passwort

# Empfänger für Zertifikate
RECIPIENT_EMAIL=modic@akademie.bpi.ac.at

# ============================================
# Beispiele für verschiedene E-Mail-Provider
# ============================================

# --- Office 365 / Microsoft 365 ---
# SMTP_HOST=smtp.office365.com
# SMTP_PORT=587
# SMTP_SECURE=false

# --- Gmail (App-Passwort erforderlich!) ---
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# Hinweis: 2FA aktivieren, dann App-Passwort erstellen

# --- GMX ---
# SMTP_HOST=mail.gmx.net
# SMTP_PORT=587
# SMTP_SECURE=false
